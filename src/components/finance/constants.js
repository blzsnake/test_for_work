import { FormattedMessage } from 'react-intl';
import React from 'react';

export const PAYMENTS = {
  titles: ['ID', <FormattedMessage id='finance_date' />, <FormattedMessage id='title' />, <FormattedMessage id='finance_sum' />, ' '],
  rows: [],
};

export const PAYMENTS_NO_BTN = {
  titles: ['ID', <FormattedMessage id='finance_date' />, <FormattedMessage id='title' />, <FormattedMessage id='finance_sum' />],
  rows: [],
};
